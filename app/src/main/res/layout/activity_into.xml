<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryLight"
    tools:context=".ui.activity.IntoActivity">

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_centerHorizontal="true"
        android:layout_width="@dimen/intro_view_pager_width"
        android:layout_height="@dimen/intro_view_pager_height"/>

    <me.relex.circleindicator.CircleIndicator
        android:id="@+id/indicator"
        android:layout_width="@dimen/intro_indicator_width"
        android:layout_height="@dimen/intro_indicator_height"
        android:layout_below="@id/viewPager"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/indicator"
        android:textColor="@android:color/white"
        android:textSize="@dimen/xxxText"
        android:text="@string/welcome"/>

</RelativeLayout>